{
  "name": "chatjimmy2api-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "cloudflare": {
    "bindings": {
      "OPENAI_API_KEY": {
        "type": "secret_text",
        "description": "Bearer token clients must send in Authorization header."
      }
    }
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/proxy.js && node --check src/worker.js && node --check api/index.js && node --check api/[...path].js && node --check netlify/functions/openai.js"
  },
  "devDependencies": {
    "wrangler": "^4.52.0"
  }
}
